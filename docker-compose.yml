version: "3"
services:
    api:
        container_name: todo-api
        image: ezraamos/todo-api:${IMAGE_TAG}
        working_dir: /app
        ports:
            - "8000:8000"
        env_file:
            - .env
